          //Block for Displaying Beacon Names in SideMenu
          var i;
          var len = $scope.rangebeacons.length;
          for(i = 0; i < len; i++){
                if($scope.rangebeacons[i].major != null && $scope.rangebeacons[i].minor != null){
                    var displayMajor = $scope.rangebeacons[i].major; //Temp Major 
                    var displayMinor = $scope.rangebeacons[i].minor; //Temo Minor
                    //COMPARE rangeBeacons to API data
                    if(displayMajor == $scope.data[i].major && displayMinor == $scope.data[i].minor){
                      //STORE API data.title and data.url                      
                      $scope.tempData = {
                                          "title":$scope.data[i].title,
                                          "link": $scope.data[i].url
                                         };
                      //PUSH DATA to new array to display
                      $scope.displayBeacons.push($scope.tempData);
                      console.log($scope.displayBeacons.title);
                    }
                    
                }
          }